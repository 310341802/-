<form>
  <view class="section">
  <picker bindchange="bindPickerChange"  range-key="{{'name'}}" value="{{index}}" range='{{objectArray}}'>
    <view class="picker">第1题：{{objectArray[index].name}}</view>
  </picker>
</view>
<button form-type='submit'>提交</button>
</form>
