<!--pages/my/mes/mes.wxml-->
<view class="row" wx:for="{{mess}}">
  <view class="row-img"><image src="{{item.img}}"></image></view>
  <view class="row-desc">
    <view class="con">
      <view class="name">{{item.name}}</view>
      <view class="desc">{{item.con}}</view>
    </view>
    <view class="time">{{item.time}}</view>
  </view>
</view>
